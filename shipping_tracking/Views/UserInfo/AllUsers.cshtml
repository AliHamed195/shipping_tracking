@*@model IEnumerable<shipping_tracking.Models.User>

@{
    ViewData["Title"] = "AllUsers";
}

<style>
    .user-image {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        object-fit: cover;
    }
</style>


<h1>All Users</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Image
            </th>
            <th>
                Name
            </th>
            <th>
                Email
            </th>
            <th>
                Role
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr id="user-row-@item.Id">
                <td>
                    <img src="@item.ImagePath" alt="User Image" class="user-image" />
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                    @item.Email
                </td>
                <td>
                    @item.Role?.Name
                </td>

                <td>
                    <a asp-action="UpdateUser" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a onclick="deleteUser(@item.Id)">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>


@if (TempData["successMessage"] != null)
{
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            Swal.fire({
                position: 'top-end',
                title: 'Done...',
                text: '@TempData["successMessage"]',
                icon: 'success',
                showConfirmButton: false,
                timer: 2000
            });
        });

        @TempData["successMessage"] = null;
    </script>
}

@if (TempData["ErrorMessage"] != null)
{
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            Swal.fire({
                position: 'top-end',
                title: 'Oops!!'
                text: '@TempData["ErrorMessage"]',
                icon: 'error',
                showConfirmButton: false,
                timer: 2000
            });
        });

        @TempData["ErrorMessage"] = null;
    </script>
}

<script>
    function deleteUser(id) {
        Swal.fire({
            title: 'Are you sure?',
            text: 'You will not be able to recover this user!',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                $.ajax({
                    url: '/User/Delete/' + id,
                    type: 'POST',
                    success: function (response) {
                        if (response.success) {
                            $('#user-row-' + id).remove();
                            Swal.fire(
                                'Deleted!',
                                'Your User has been deleted.',
                                'success'
                            );
                        } else {
                            Swal.fire(
                                'Error!',
                                response.message,
                                'error'
                            );
                        }
                    },
                    error: function (xhr, ajaxOptions, thrownError) {
                        Swal.fire(
                            'Error!',
                            'An error occurred while deleting the User.',
                            'error'
                        );
                    }
                });
            }
        });
    }
</script>*@